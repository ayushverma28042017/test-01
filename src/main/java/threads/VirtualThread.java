//package threads;
//
//public class VirtualThread {
//
//   // Kotline: //subroutine , coroutine, continuation
//    // left and came back, still rem detiasl of state , >> continuaiton
//    // continuation: it is a DS that help to re-store the contect of a call betwen calls to a co-routine
//    // but ue done ahve that contiunda DS .
//    // suspend compute() // it will have contiunatione, compile know thie can be suspeded, so need to store state
//    // javap: bytecode:
//    // make a remote call and block
//    // mmake a remote call and unmount
//
//    public static void main(String [] args) {
//
//        int MAX=100;
//        for(var i=0;i<10;i++)
//    }
//
//}
// VT is like qtips: use and throw, dont pool thme
// Exceutoreservice shdu not be confused with Pool
// thread are expensive, GC , so pooling is require
// VT: light, mounted and umounted
// make scabale: easy to code ..

// in Java 8
// Excuters.newVirtualPerTaskExecutor();
